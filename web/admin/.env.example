# Backend the admin SPA talks to (see src/api.ts). Copy this file to
# .env.local and adjust for your environment; .env.local is gitignored.
# In production the admin SPA is served same-origin from /admin, so this is
# only needed for local dev against a separately-running backend.
VITE_ADMIN_API_URL=http://localhost:8080
